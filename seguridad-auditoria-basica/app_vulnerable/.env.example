# VULNERABILIDAD (SEC-011): este archivo deberia traer los valores vacios para
# que cada desarrollador complete los suyos, pero trae claves reales de ejemplo.
# Si se copia tal cual, la app queda usando claves publicadas en el repositorio.
# Uso: copiar a .env y completar (nunca subir el .env resultante).
DB_USER=admin
DB_PASSWORD=Admin123
DB_HOST=localhost
DB_NAME=app_demo
API_KEY=sk_EJEMPLO_REDACTADO_PARA_REPO_PUBLICO
SECRET_JWT=cambiar_esto_en_produccion
DEBUG=True
